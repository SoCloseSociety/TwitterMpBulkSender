# SoClose X/Twitter DM Sender - Configuration
# Copy this file to .env and adjust values as needed

# Delay between messages (seconds) - randomized between min and max
DELAY_MIN=3
DELAY_MAX=7

# Selenium timeout for element detection (seconds)
TIMEOUT=15

# Maximum retry attempts per profile
MAX_RETRIES=3

# Message file path
MESSAGE_FILE=message.txt
